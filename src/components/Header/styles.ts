import styled from "styled-components/native";

export const HeaderV = styled.View` 
    background-color: ${({theme}) => theme.COLORS.VIOLET};
    flex: 1;
    max-width: 130px;
    min-width: 130px;
`;

